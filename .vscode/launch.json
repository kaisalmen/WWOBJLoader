
{
    "version": "5.0.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:8085/packages/examples/index.html",
            "webRoot": "${workspaceFolder}"
        }
    ]
}
