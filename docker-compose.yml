version: '3'

services:
  obj2dev:
    image: obj2dev
    build:
      dockerfile: docker/Dockerfile
      context: .
    ports:
      - 8085:8085
    volumes:
      - ./:/workspace/WWOBJLoader/:rw
    command: ["bash", "/workspace/WWOBJLoader/docker/runDevEnv.sh"]
    container_name: obj2dev
